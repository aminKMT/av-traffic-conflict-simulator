{
  "name": "av-traffic-conflict-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based AV traffic conflict simulator for TTC, PET, collision and near-conflict exploration.",
  "scripts": {
    "start": "python3 -m http.server 8080 -d app",
    "check": "node scripts/check-repo.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
